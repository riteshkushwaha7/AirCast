# Alembic migrations

This directory is reserved for migration scripts.

Recommended next step:
1. `alembic init migrations` (if not initialized)
2. configure `alembic.ini` and `env.py`
3. autogenerate revision from SQLAlchemy models
